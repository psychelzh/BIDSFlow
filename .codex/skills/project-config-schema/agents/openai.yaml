interface:
  display_name: "Project Config Shape"
  short_description: "Keep scaffold and config terminology aligned"
  default_prompt: "Use $project-config-schema to update the BIDSFlow config shape and keep scaffold expectations, terminology, and future schema design aligned."
